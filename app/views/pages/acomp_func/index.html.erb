   
           <div class="content">
                <div class="container-fluid">
                    <div class="row">
                        <div class="col-md-12">
                            <div class="card strpied-tabled-with-hover">
                                <div class="card-header ">
                                <hr>
                                    <h4 class="card-title">Relação de Justificativas por Subordinado</h4>
                                <hr>    
                                 <%= form_tag pages_acomp_func_index_path do %>
                                   <div class="row">
                                    <div class="col-2">
                                       <label>Chapa:</label>
                                       <%= text_field_tag :chapa , "", class: 'form-control' %>
                                     </div>
                                     <div class="col-3">
                                       <label>Informe uma Data Inicial :</label>
                                       <%= date_field_tag :data_ini , "", class: 'form-control' %>
                                     </div>
                                     <div class="col-3">
                                       <label>Informe uma Data Final : </label>
                                       <%= date_field_tag :data_fim , "", class: 'form-control'%> 
                                     </div>
                                    
                                   </div>
                                      <div class="col-3">
                                       <%= submit_tag "Enviar" , class: 'btn btn-primary'%> 
                                      </div>
                                 <% end %>
                                       
                                </div>
                                   
                                <div class="card-body table-responsive">
                                 <hr>
                                    <h4 class="card-title">Detalhamento</h4>
                                    <table class="table table-hover">
                                        <thead>
                                            <th>Chapa</th>
                                            <th>Data</th>
                                            <th>Inicio</th>
                                            <th>Fim</th>
                                            <th>Total</th>
                                            <th>Justificativa</th>
                                        </thead>
                                        <% if @result.present? %>
                                         <% @result.each do |acompfunc| %> 
                                        <tbody>
                                            <tr>
                                                <th><%= acompfunc.try(:CHAPA).try(:content)%></th>
                                                <th><%= acompfunc.try(:DATA).try(:content) %></th>
                                                <th><%= acompfunc.try(:INICIO).try(:content)%></th>
                                                <th><%= acompfunc.try(:FIM).try(:content)%></th>
                                                <th><%= acompfunc.try(:TOTAL).try(:content) %></th>
                                                <th><%= acompfunc.try(:JUSTIFICATIVA).try(:content)%></th>
                                            </tr>

                                            <% end %>           
                                         <% end %> 
                                        </tbody>
                                    </table>
                                </div>
                            </div>
                        </div>
                          
        </div>
    </div>

